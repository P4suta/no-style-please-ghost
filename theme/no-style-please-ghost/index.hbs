{{!< default}}

{{!-- Fallback listing used whenever no more-specific template matches
  (e.g. non-home collection routes). `home.hbs` handles the front page
  and carries the site title / featured block; this file stays minimal. --}}
{{> post-list}}

{{pagination}}
